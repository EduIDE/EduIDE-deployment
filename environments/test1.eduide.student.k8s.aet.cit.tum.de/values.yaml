# Helm values for the test1 environment.
#
# A plain values file. The deploy runs:
#   helm upgrade --install eduide ... -f environments/_base.yaml -f this file
#
# _base.yaml carries what is identical everywhere; this file carries what is
# not. Anything appearing here for more than one environment belongs in
# _base.yaml instead.
#
# Deployment metadata - which cluster, which namespace, which chart version -
# is in env.yaml next door, because it configures the deploy rather than the
# chart.

app:
  name: Artemis Online IDE (Test1)
gateway:
  parentRefs:
  - name: theia-shared-gateway
    namespace: eduide-system
    sectionName: test1-landing
  - name: theia-shared-gateway
    namespace: eduide-system
    sectionName: test1-service
  - name: theia-shared-gateway
    namespace: eduide-system
    sectionName: test1-instances
  - name: theia-shared-gateway
    namespace: eduide-system
    sectionName: test1-webview
landingPage:
  footerLinks:
    attribution:
      version: Test1-v1.0.0
  infoTitle: Welcome to EduIDE Cloud (Test1)
hosts:
  # --- hostnames served by this installation (checked by test-deploy-logic.sh)
  #   test1.eduide.student.k8s.aet.cit.tum.de                      landing page
  #   service.test1.eduide.student.k8s.aet.cit.tum.de              REST service
  #   instance.test1.eduide.student.k8s.aet.cit.tum.de             session ingress
  #   *.webview.instance.test1.eduide.student.k8s.aet.cit.tum.de   session webviews
  # --- end hostnames
  configuration:
    baseHost: eduide.student.k8s.aet.cit.tum.de
    service: service.test1
    landing: test1
    instance: instance.test1
  allWildcardInstances:
  - '*.webview.'
keycloak:
  enable: true
  authUrl: https://keycloak-test.aet.cit.tum.de/
  realm: tum
  clientId: eduide
eduide-shared-cache:
  enabled: true
